# 人情账 / Gift Ledger v1.2.13

## v1.2.13 修复内容

1. 修复所有配置页面移动端左滑失效的问题。
2. 覆盖以下配置页面：
   - 人员管理
   - 关系配置
   - 分类配置
   - 类型管理
   - 红包规则
   - 汇率设置
3. 现在在输入框、下拉框区域横向左滑，也可以正常显示删除按钮。
4. 点击输入框仍然可以正常编辑；只有横向滑动时才触发左滑操作。
5. 保留移动端主页面记录卡片左滑显示操作按钮。
6. 左滑展开时，悬浮按钮仍会自动隐藏。
7. JS 语法已检查通过。
8. 代码内版本号已同步为 v1.2.13。

## GitHub 需要更新

```text
index.html
README.md
```

## Cloudflare 需要更新

```text
不需要更新 worker.js
```

## data.json

```text
不要覆盖线上 data.json
```
