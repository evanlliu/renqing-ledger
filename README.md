# 人情记账本 / Gift Ledger v1.2.4

## v1.2.4 更新内容

1. 移动端配置管理页面拖动按钮位置优化。
2. 人员管理、关系配置、分类配置、类型管理、红包规则、汇率设置中，拖动按钮改到文字/输入区域最左边。
3. 拖动按钮不再单独占一整行，避免显示在文字上方。
4. 删除按钮继续保持左滑显示，并保留二次确认。
5. 代码内版本号已同步为 v1.2.4。

## 更新说明

已有线上数据时，只更新：

```text
index.html
README.md
```

不要覆盖线上 `data.json`。

Cloudflare Worker 无变化，不需要更新 `worker.js`。


## v1.2.4

- 修复 iOS Safari / 添加到主屏幕后日期输入框宽度超出问题。
- 统一修复新增、筛选、人员管理及所有配置页面中的 date/time 类输入控件宽度。
- 增强移动端表单、弹窗和配置行的横向溢出控制，避免左右晃动。


## v1.2.4

- 优化移动端弹窗滚动锁定。
- 弹出新增、编辑、配置管理等页面时，禁止主页面跟随滑动。
- 修复 iOS Safari / 添加到主屏幕后弹窗滚动穿透问题。
