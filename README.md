# 人情记账本 / Gift Ledger v1.1.9

## v1.1.9 更新内容

1. 移动端配置管理页面优化删除交互。
2. 人员管理、关系配置、分类配置、类型管理、红包规则、汇率设置中的删除按钮，移动端默认隐藏。
3. 移动端左滑配置行后显示删除按钮，降低误触风险。
4. 右滑或点击其他配置行时自动收起已展开的删除按钮。
5. PC 端仍保留原来的删除按钮展示方式，方便快速管理。

## 更新说明

已有线上数据时，只更新 `index.html` 和 `README.md`，不要覆盖线上 `data.json`。

Cloudflare Worker 无变化，不需要更新 `worker.js`。
